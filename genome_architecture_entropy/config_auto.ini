####################
#   Spring Model   #
####################

# This is automatically generated config file.
# Generated at: 2022-03-01T09:52:50.530559

# Notes:
# Some fields require units. Units are represented as objects from simtk.units module.
# Simple units are parsed directly. For example: 
# HR_R0_PARAM = 0.2 nanometer
# But more complex units does not have any more sophisticated parser written, and will fail.'
# In such cases the unit is fixed (and noted in comment), so please convert complex units manually if needed.
# <float> and <int> types does not require any unit. Quantity require unit.

# Default values does not mean valid value. In many places it's only a empty field that need to be filled.

[Main]; name of the platform. Available choices: Reference CPU OpenCL, type: str, default: 
PLATFORM = 

; device index for CUDA or OpenCL (count from 0), type: str, default: 
DEVICE = 

; Path to PDB file., type: str, default: 
INITIAL_STRUCTURE_PATH = /Users/pita/Documents/Rene/GAM_project/genome_architecture_entropy/toymodel/md_soft/out/min_struct.pdb

; Path to XML file with forcefield., type: str, default: 
FORCEFIELD_PATH = /Users/pita/Documents/Rene/GAM_project/md_soft/forcefields/classic_sm_ff.xml

; Use harmonic bond interaction., type: bool, default: True
POL_USE_HARMONIC_BOND = True

; harmonic bond distance equilibrium constant, type: Quantity, default: 0.1 nanometer
POL_HARMONIC_BOND_R0 = 0.1 nanometer

; harmonic bond force constant (fixed unit: kJ/mol/nm^2), type: float, default: 300000.0
POL_HARMONIC_BOND_K = 300000.0

; Use fixed bond length instead of harmonic interaction, type: bool, default: False
POL_USE_CONSTRAINTS = False

; Fixed constraints length, type: Quantity, default: 0.1 nanometer
POL_CONSTRAINT_DISTANCE = 0.1 nanometer

; Use harmonic angle interaction., type: bool, default: True
POL_USE_HARMONIC_ANGLE = True

; harmonic angle distance equilibrium constant, type: float, default: 3.141592653589793
POL_HARMONIC_ANGLE_R0 = 3.141592653589793

; harmonic angle force constant (fixed unit: kJ/mol/radian^2), type: float, default: 10.0
POL_HARMONIC_ANGLE_K = 0.02

; Use excluded volume., type: bool, default: False
EV_USE_EXCLUDED_VOLUME = True

; Epsilon parameter., type: float, default: 2.86
EV_EPSILON = 2.86

; Sigma parameter., type: Quantity, default: 0.05 nanometer
EV_SIGMA = 0.05 nanometer

; Use long range interactions or not, type: bool, default: False
HR_USE_HARMONIC_RESTRAINTS = True

; Use flat bottom force instead of standard harmonic force., type: bool, default: False
HR_USE_FLAT_BOTTOM_FORCE = True

; Path to .rst file with indices, type: str, default: 
HR_RESTRAINTS_PATH = /Users/pita/Documents/Rene/GAM_project/genome_architecture_entropy/toymodel/md_soft/restraints/restraint6.rst

; distance constant, this value will be used only if it is missing in rst file, type: Quantity, default: 
HR_R0_PARAM = 0.1 nanometer

; force constant, this value will be used only if it is missing in rst file (Fixed unit: kilojoule_per_mole/nanometer**2 - only float number needed.), type: float, default: 
HR_K_PARAM = 300000.0 kJ/(nm**2 mol)

; force constant multiplicator applied if you are using values from rst file., type: float, default: 1
HR_K_SCALE = 1.0

; Use Spherical container, type: bool, default: False
SC_USE_SPHERICAL_CONTAINER = True

; Spherical container location x, fixed unit: nanometers, type: float, default: 
SC_CENTER_X = 0.0

; Spherical container location y, fixed unit: nanometers, type: float, default: 
SC_CENTER_Y = 0.0

; Spherical container location z, fixed unit: nanometers, type: float, default: 
SC_CENTER_Z = 0.0

; Spherical container radius, fixed unit: nanometers, type: float, default: 30
SC_RADIUS = 100.0

; Spherical container scaling factor, type: float, default: 1000
SC_SCALE = 1000.0

; should initial structure be minimized? - This is spring model main functionality., type: bool, default: True
MINIMIZE = True

; If left empty result file will have name based on initial structure file name with _min.pdb ending., type: str, default: 
MINIMIZED_FILE = /Users/pita/Documents/Rene/GAM_project/genome_architecture_entropy/toymodel/md_soft/out/min_struct.pdb

; Do you want to run MD simulation?, type: bool, default: False
SIM_RUN_SIMULATION = False

; Alternative: langevin, verlet, type: str, default: verlet
SIM_INTEGRATOR_TYPE = verlet

; Friction coefficient (Used only with langevin integrator), type: float, default: 
SIM_FRICTION_COEFF = 

; Number of steps in MD simulation, type: int, default: 
SIM_N_STEPS = 10000

; Time step (use time unit from simtk.unit module), type: Quantity, default: 
SIM_TIME_STEP = 150.0 femtosecond

; Temperature (use temperature unit from simtk.unit module), type: Quantity, default: 
SIM_TEMP = 310.0 kelvin

; Random seed. Set to 0 for random seed., type: int, default: 0
SIM_RANDOM_SEED = 0

; Sets initial velocities based on Boltzmann distribution, type: bool, default: False
SIM_SET_INITIAL_VELOCITIES = True

; Number of trajectory frames to save., type: int, default: 2000
TRJ_FRAMES = 1000

; Write trajectory in DCD file format, leave empty if you do not want to save., type: str, default: 
TRJ_FILENAME_DCD = example_data/example_result/trj.dcd

; Write trajectory in PDB file format, leave empty if you do not want to save., type: str, default: 
TRJ_FILENAME_PDB = example_data/example_result/trj.pdb

; Write last frame of trajectory in PDB file format, leave empty if you do not want to save., type: str, default: 
TRJ_LAST_FRAME_PDB = 

; Number of states reported on screen, type: int, default: 20
REP_STATE_N_SCREEN = 20

; Number of states reported to file screen, type: int, default: 1000
REP_STATE_N_FILE = 1000

; Filepath to save state. Leave empty if not needed., type: str, default: state.csv
REP_STATE_FILE_PATH = example_data/example_result/state.csv

; H5 file to save velocities. Leave empty if not needed., type: str, default: state.h5
REP_STATE_FILE_H5_PATH = state.h5

; Filepath to save energy plot. Leave empty if not needed., type: str, default: energy.pdf
REP_PLOT_FILE_NAME = example_data/example_result/energy.pdf

; External force, type: bool, default: False
EF_USE_EXTERNAL_FIELD = False

; npy file, that defines regular 3D grid with external field values, type: str, default: 
EF_PATH = 

; External Field Voxel size X, type: Quantity, default: 
EF_VOXEL_SIZE_X = 

; External Field Voxel size Y, type: Quantity, default: 
EF_VOXEL_SIZE_Y = 

; External Field Voxel size Z, type: Quantity, default: 
EF_VOXEL_SIZE_Z = 

; Should the field be normalized to [0;1]?, type: bool, default: False
EF_NORMALIZE = False

; External field scaling factor, type: float, default: 1.0
EF_SCALING_FACTOR = 1.0